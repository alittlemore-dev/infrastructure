worker_processes auto;
pid /tmp/nginx.pid;
error_log /dev/stderr warn;

events { worker_connections 1024; }

http {
    map $uri $privacy_safe_uri {
        default $uri;
        ~^/api/personal-workspace/knowledge(?:/|$) "/api/personal-workspace/knowledge/{private}";
    }

    log_format privacy_safe '$request_method $privacy_safe_uri $server_protocol $status';
    access_log /dev/stdout privacy_safe;
    log_format agent_api_json escape=json
        '{"timestamp":"$time_iso8601",'
        '"method":"$request_method","uri":"$uri","status":$status,'
        '"requestId":"$request_id","clientVerify":"$ssl_client_verify",'
        '"clientFingerprint":"$ssl_client_fingerprint"}';

    proxy_temp_path /tmp/proxy_temp;
    client_body_temp_path /tmp/client_temp;
    fastcgi_temp_path /tmp/fastcgi_temp;
    uwsgi_temp_path /tmp/uwsgi_temp;
    scgi_temp_path /tmp/scgi_temp;

    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;
    client_max_body_size 100m;

    gzip on;
    gzip_disable "msie6";
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types
        text/plain text/css application/json application/javascript
        text/xml application/xml application/xml+rss text/javascript;

    include /tmp/nginx-conf.d/*.conf;
}
